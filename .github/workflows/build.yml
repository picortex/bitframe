name: Integration & Acceptance Testing

on:
  pull_request:
    branches: [ master-dev ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 16
        uses: actions/setup-java@v1
        with:
          java-version: 16

      - uses: browser-actions/setup-chrome@latest

      - name: Cache Gradle
        uses: actions/cache@v2
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
            ~/.gradle/yarn
            ~/.gradle/nodejs
          key: ${{ runner.os }}-gradle-${{ hashFiles('gradle/gradle-wrapper.*') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Cache konan
        uses: actions/cache@v2
        with:
          path: |
            ~/.konan/dependencies
            ~/.konan/kotlin-native-macos*
          key: ${{ runner.os }}-konan-
          restore-keys: ${{ runner.os }}-konan-

      - name: Making gradlew executable
        run: chmod +x ./gradlew
        working-directory: .

      - name: Download gradle
        run: ./gradlew --version

      - name: Prepare integration tests
        run: TEST_MODE=CI ./gradlew :pi-monitor-server:createDockerfile
        working-directory: .

      - name: Running integration tests
        run: TEST_MODE=CI ./gradlew jvmTest --tests integration.*
        working-directory: .

      - name: Running acceptance tests
        run: TEST_MODE=CI HEADLESS=true ./gradlew jvmTest --tests acceptance.*
        working-directory: .